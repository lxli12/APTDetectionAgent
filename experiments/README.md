# Experiment definitions

Tracked experiment recipes are added here only after approval. Generated runs,
checkpoints, logs, metrics, and artifacts belong under the external append-only run
root and are never committed.

Every recipe must record REQ-REPRO-001..003, the main-project SHA, PIDSMaker SHA,
resolved configuration hashes, dataset hashes, resource profile, and run ID.
