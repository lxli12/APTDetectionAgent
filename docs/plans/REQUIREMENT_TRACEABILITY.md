# Requirement Traceability Matrix

Status values: `implemented`, `partial`, `planned`, `blocked`. Every implementation
and test path is updated when work lands.

| ID | Invariant | Phase | Implementation evidence | Verification | Status |
|---|---|---:|---|---|---|
| REQ-GOV-001 | Every module, test, and acceptance item cites a requirement ID | 0 | `AGENTS.md`; this matrix | `tests/test_governance.py` | implemented |
| REQ-GOV-002 | Phase completion requires invariant evidence, not only exit code zero | 0 | `IMPLEMENTATION_PLAN.md` | governance review | implemented |
| REQ-GOV-003 | Final design and accepted ADRs are behavioral sources of truth | 0 | `AGENTS.md`; `docs/PROJECT_ARCHITECTURE_DESIGN_v1.1.md` | governance checker and schema dependency-floor checks | implemented |
| REQ-GOV-004 | SFT work is explicitly blocked when no valid dataset exists | 0/10 | plan; experiment protocol; formal stage orchestrator | unit negative test and AutoDL blocked preflight artifact | implemented |
| REQ-GIT-001 | Tracked code changes locally and reaches server only through push + fast-forward pull | 0 | `AGENTS.md` | remote workflow review | implemented |
| REQ-GIT-002 | Dirty remote worktree stops synchronization; no destructive Git | 0/10 | `AGENTS.md`; `scripts/remote/sync_code.sh` | dirty-tree pre-network failure and destructive-command scan | implemented |
| REQ-GIT-003 | PIDSMaker remains pinned and unmodified | all | `.gitmodules`; commit pin | submodule status check | implemented |
| REQ-CAUSAL-001 | No future window or event may enter current computation | 3 | `data/windows.py`; `data/causal.py` | future/boundary/ordering negative tests | implemented |
| REQ-CAUSAL-002 | Vocabulary, normalization, IDF, statistics, embeddings, model, and threshold freeze before held-out | 1/3 | `data/causal.py:FittedStateArtifact` | split/refit/freeze-cutoff tests | implemented |
| REQ-CAUSAL-003 | Current-graph parameter-free features are allowed only after that window arrives | 3 | `data/causal.py:CausalFeatureBoundary` | arrival and current-event timestamp tests | implemented |
| REQ-CAUSAL-004 | Transductive runs are labeled compatibility baselines and excluded from main results | 1/2/3/7/9 | config, fitted-state, standardized-result, and comparison gates | mismatched result rejection, mixed-comparison rejection, and false-formal-claim tests | implemented |
| REQ-LABEL-001 | Agent-visible processes cannot access hidden labels or private mappings | 1/3/7/9 | strict public schemas; separate evaluator namespace/process; role-specific runtime trees | nested leakage, subprocess output, and live OS permission checks | implemented |
| REQ-LABEL-002 | Hidden teacher data is sanitized before student input, rationale, or deployable memory | 1/4/10 | recursive deployable-payload guard; SFT sanitizer; runtime memory/report tool guard | privileged-field/phrase, static-LTM-write, and teacher-to-student negative tests | implemented |
| REQ-LABEL-003 | Held-out evaluator returns no online step reward; only episode metrics | 1/7 | `evaluation/public.py`; evaluator IPC | subprocess/public-payload tests | implemented |
| REQ-LABEL-004 | Trace routing uses deployment-visible evidence only | 1/2/5/7 | AgentAction evidence IDs; strict trace/controller guards | hidden annotation/rationale rejection tests | implemented |
| REQ-WINDOW-001 | Windows record origin, timezone, size, and `[start,end)` bounds | 1/3 | `schemas/runtime.py:TimeWindow` | alignment/timezone/boundary tests | implemented |
| REQ-WINDOW-002 | Scenario processing is chronological and predictions are append-only | 3/5 | `data/windows.py:CausalWindowStream` | multi-window, skip, advance, replay/rewrite tests | implemented |
| REQ-WINDOW-003 | Current window uses committed fast-path configuration | 1/3/5 | `CaseState`; `PendingConfiguration`; causal stream | prediction/config mismatch test | implemented |
| REQ-WINDOW-004 | Rolling-range constants are validation candidates, not hard-coded truths | 3/5 | `data/causal.py:RollingRangeCandidate` | non-validation source rejection | implemented |
| REQ-CONFIG-001 | Persistent reconfiguration takes effect at the next window | 1/5 | effective-window validators | same-window rejection tests | implemented |
| REQ-CONFIG-002 | Held-out selects only frozen ApprovedConfig and thresholds | 1/2/5 | `schemas/pids.py`; `pidsmaker/tools.py:ApprovedConfigCatalog` | schema and frozen-catalog rejection tests | implemented |
| REQ-CONFIG-003 | Threshold records carry method, split, checkpoint, metric, time, and commit provenance | 1 | `ThresholdProvenance` | source/missing-field tests | implemented |
| REQ-PIDS-001 | Registry covers every PIDS/variant at the pinned commit | 2 | `pidsmaker/registry.py`; `docs/pidsmaker/MODEL_INVENTORY.md` | dynamic config/dataset/variant parity tests | implemented |
| REQ-PIDS-002 | Unavailable PIDS remain registered with explicit reasons | 1/2 | `PIDSCapability`; discovery checkpoint inventory | missing-root retains all entries test | implemented |
| REQ-PIDS-003 | ORTHRUS fixed/non-snooped are variants, not new methods | 1/2 | normalized `PIDSRef` | identity negative tests | implemented |
| REQ-PIDS-004 | `feat_inference` is traced internally unless a real Agent decision justifies a tool | 2 | `docs/pidsmaker/PIPELINE_MAP.md`; discovery stage map | internal-stage registry test | implemented |
| REQ-PIDS-005 | PIDSMaker submodule is never directly modified | all | adapter boundary | submodule diff check | implemented |
| REQ-PIDS-006 | Formal real trajectories require scoped causal-config, checkpoint, threshold, parser, resource, state/reset, real-smoke, and provenance gates | 5/8/9/10 | `schemas/admission.py`; frozen runtime catalog | all-gates, single-failure, synthetic-smoke, and catalog-scope negative tests | implemented; no real candidate currently admitted |
| REQ-RUNTIME-001 | Harness commits the current fast-path result before trigger/LLM work; untriggered windows create no assistant turn | 5/9 | `runtime/controller.py`; committed ledger | zero-call untriggered test; exactly-once duplicate test; AutoDL frozen synthetic smoke | implemented |
| REQ-RUNTIME-002 | Raw, complete canonical, and token-budgeted prompt observations are distinct deterministic hashed layers | 5/10 | `schemas/agent_runtime.py`; `runtime/observation.py` | hash mutation, trigger timing, prompt budget, and hidden-field tests | implemented |
| REQ-RUNTIME-003 | Runtime actions use the exact eight-action taxonomy and opaque approved choices | 5/10 | `agent/policy.py`; `ProposedAction`; runtime-owned `RuntimeActionValidator`; `ExecutableAction`; action-to-tool map | proposal privilege-field rejection, evidence/action mismatch, future-timing, and tool-boundary negative tests | implemented |
| REQ-RUNTIME-004 | Triggered diagnosis uses read request, deterministic retrieval result, memory-use decision/action, and optional write candidate | 4/5/10 | `schemas/memory_runtime.py`; `memory/protocol.py` | needed=false turn-shape, exact disposition, used-ID citation, leakage tests | implemented |
| REQ-RUNTIME-005 | Additional detector output is supplemental and can never replace the committed current-window result | 5/9 | separate request/result/transaction fields | committed literal, preflight rejection, result-ID, and re-prompt tests | implemented |
| REQ-RUNTIME-006 | Persistent threshold/config/detector/resource changes activate only at an admitted future window | 5 | frozen pending state and `prepare_case_for_window` | current-state preservation, pending overwrite, and next-window activation tests | implemented |
| REQ-TOOL-001 | LLM emits typed requests and never constructs shell commands | 1/2/5 | `AgentAction`; `ToolRequest` recursive guard | executor-field/unknown-field tests | implemented |
| REQ-TOOL-002 | Executor validates parameter/path/resource allowlists and builds argv | 2 | `pidsmaker/adapter.py` | override, path, checkpoint, shell-boundary tests | implemented |
| REQ-TOOL-003 | Every tool call records validated args, config/checkpoint, command manifest, timing, output, and artifacts | 1/2/5/8 | `ToolResult`; `CommandManifest`; `pidsmaker/adapter.py`; memory/case/report audit JSONL | structured-tool tests plus real AutoDL run `structured-adapter-20260714-007` with five-stage trace and hashed artifacts | implemented |
| REQ-TOOL-004 | Parallel PIDS selection is scheduled by executor, never CUDA-selected by LLM | 2/5 | PIDSMaker tool scheduler; `runtime/scheduler.py` | GPU assignment/serialization and hidden CUDA tests | implemented |
| REQ-TOOL-005 | Timeout, nonzero exit, malformed output, and missing artifact fail closed | 2/5/8 | `pidsmaker/adapter.py`; `tools/memory.py`; `scripts/pidsmaker_stage_runner.py` | negative tests; preserved typed failures `structured-adapter-20260714-002/004/005`; real typed parser success `structured-adapter-20260714-007` | implemented |
| REQ-MEMORY-001 | Working, episode memory, and case state reset at split/scenario boundaries | 1/4 | `memory/store.py:CaseMemoryStore`; scoped schemas | exact episode reset tests | implemented |
| REQ-MEMORY-002 | Train memory cannot enter validation; validation cannot enter held-out | 1/4 | `MemoryNamespace`; runtime write scope gate | cross-split/scenario negative tests | implemented |
| REQ-MEMORY-003 | Frozen deployable static LTM may cross splits only as a sanitized training artifact | 1/4 | `StaticLTMSnapshot`; `StaticLTMSanitizer` | signature/review/schema/leakage tests | implemented |
| REQ-MEMORY-004 | Runtime held-out memory cannot update static LTM | 1/4 | static snapshot loader; runtime write rejection | static immutability/reset survival tests | implemented |
| REQ-MEMORY-005 | Initial backend is SQLite FTS5 with normalized-hash exact dedup | 4 | `memory/store.py:MemoryStore` | FTS retrieval and normalized dedup tests | implemented |
| REQ-MEMORY-006 | Conflicts coexist with environment, time, and evidence provenance | 4 | semantic key plus explicit conflict/evidence fields | conflict coexistence/unknown-target tests | implemented |
| REQ-MEMORY-007 | Retrieval budget and candidate cap are validation-tuned engineering defaults | 4/9 | `RetrievalPolicy`; evaluator-only `run_memory_retrieval_sensitivity.py` | cap/budget/false-optimal-claim tests and synthetic harness smoke; formal validation query manifest still required | partial |
| REQ-EVAL-001 | Main evaluation uses campaign coverage and unique malicious-node TP/FP/FN | 1/7 | `evaluation/metrics.py` privileged engine | exact synthetic metric fixture | implemented |
| REQ-EVAL-002 | Node-window, edge, chain, phase, provenance, latency, GPU, and tool-call metrics have separate denominators | 1/7 | distinct private metric maps | denominator-isolation tests | implemented |
| REQ-EVAL-003 | P@C=100%, MCC, and ADP definitions are versioned | 1/7 | `agent-eval-v2`; metric protocol | numeric fixtures and tie-safe ranking | implemented |
| REQ-EVAL-004 | Campaign identity comes from a versioned manifest, not a ground-truth filename | 1/7 | privileged `CampaignManifest` | manifest validation tests | implemented |
| REQ-EVAL-005 | Validation calibrates coverage constraints using agent-level hidden campaigns | 7 | `evaluation/calibration.py` private validation-only workflow | exact campaign-level threshold, provenance, held-out/benign-only/unsatisfied negative tests | implemented |
| REQ-EVAL-006 | Evaluation cannot feed hidden config search during held-out | 5/7 | private evaluator process and artifact-reference feedback | subprocess leakage/overwrite tests | implemented |
| REQ-EVAL-007 | Delay, alert volume, stability, LLM efficiency, and control behavior remain separately defined | 7 | `EvaluationRecord` v2 metric maps; hidden evaluator aggregation | denominator, dispersion, cache, delay-key, and window-series tests | implemented |
| REQ-ARTIFACT-001 | Every artifact records source config, checkpoint hash, code commit, and producing stage | 1/2 | artifact and run manifests | hash/path/provenance tests | implemented |
| REQ-ARTIFACT-002 | Missing checkpoints produce unavailable status, never fabricated artifacts | 1/2/8 | checkpoint schema; discovery availability | missing-checkpoint retention tests | implemented |
| REQ-ARTIFACT-003 | Raw PIDSMaker artifacts are mapped and validated before becoming Agent contracts | 2/8 | artifact map; compatibility report; stage runner; fail-closed parser registry | missing/privileged-artifact tests, unavailable-parser rejection, and real VELOX parser run `structured-adapter-20260714-007` | implemented |
| REQ-RESOURCE-001 | Scheduler uses explicit 32 vCPU/240 GiB/2×24 GiB profile | 0/5 | AutoDL YAML; `runtime/scheduler.py` | profile load and quota rejection tests | implemented |
| REQ-RESOURCE-002 | Initial profile uses GPU 0 for vLLM and one GPU PIDS on GPU 1 | 0/5/8 | explicit executor assignments | separate-device lease test | implemented |
| REQ-RESOURCE-003 | Same-GPU concurrency waits for per-PIDS smoke profiles | 5/8 | one-unknown-workload admission gate | second same-GPU PIDS rejection | implemented |
| REQ-ENV-001 | `pids` and `vllm` environments remain separate and pinned | 0/6 | ADR and resource profile | environment manifest | implemented |
| REQ-ENV-002 | Controller communicates through subprocess and localhost HTTP, not cross-imports | 1/2/6 | PIDSMaker subprocess adapter; `agent/client.py` | transport/import-boundary tests | implemented |
| REQ-ENV-003 | vLLM host, port, base URL, and model path are environment-driven | 6 | `VLLMConfig.from_environment` | alternate-port, localhost, URL validation tests | implemented |
| REQ-ENV-004 | Controller environment stays minimal and excludes PyTorch/PyG/vLLM | 1/6 | ADR 0005; stdlib HTTP client | dependency and runtime-import tests | implemented |
| REQ-DB-001 | Admin, PIDS worker, hidden evaluator, and controller have distinct DB privileges | 0/7 | security ADR; role provision/verification scripts; split secret files | policy tests and live AutoDL role/grant verification | implemented |
| REQ-DB-002 | Controller has no private-label database access | 7 | evaluator filesystem IPC; distinct OS/DB roles and private schema policy | path/public-payload tests and live permission denial | implemented |
| REQ-DB-003 | PostgreSQL 17 data is never auto-migrated, rebuilt, or modified by runtime | 0/2 | AGENTS and ADR | command allowlist test | implemented |
| REQ-WANDB-001 | W&B is disabled, makes no network request, and is not a project dependency | 0/2/8 | AGENTS; W&B audit; disabled runner environment; isolated W&B-free training patch | dependency/source/argv tests and real import-gated causal smoke | implemented |
| REQ-REPRO-001 | Runs record exact code, environment, data, config, checkpoint, threshold, command, seed, timing, resources, metrics, and failures | 0/1/5/9 | experiment protocol; append-only synthetic, PIDS, validation, and frozen-window artifacts | complete real/synthetic run inspections | implemented |
| REQ-REPRO-002 | Every run has a unique non-overwriting run ID and required local files | 5/8/9 | stage and synthetic run collision gates; complete synthetic artifact set | overwrite-negative test and AutoDL run inspection | implemented |
| REQ-REPRO-003 | Long runs use owned tmux sessions and leave status/tail/recovery instructions | 5/8/10 | owned start/status/tail/stop/summary scripts; reproduction guide | shell tests plus live tmux PIDS runs and status/log checks | implemented |
| REQ-SFT-001 | SFT student data contains no privileged label/rationale leakage | 10 | `sft/validators.py`; strict canonical validator; read-only `sft/compat/` readers | teacher-field, prompt-hash, memory, rationale, and deployability negative tests | implemented |
| REQ-SFT-002 | Hidden teacher inputs and student-visible outputs use separate schemas | 1/10 | evaluator namespace; frozen hidden-teacher and student-example v2 schemas | privilege removal, immutable public-trace, and split-boundary tests | implemented |
| REQ-SFT-003 | Synthetic fixtures are never reported as formal training evidence | 9/10 | experiment protocol; dataset manifest; run status/report | synthetic formal-approval and checkpoint negative tests | implemented |
| REQ-SFT-004 | Missing formal trajectories yields `BLOCKED_BY_SFT_DATASET` | 0/10 | `train_sft.py`; formal training stage orchestrator | missing-dataset CLI and AutoDL preflight | implemented |
| REQ-SFT-005 | Public dataset/run manifests reference but never contain private label artifacts | 10 | `sft/models.py`; `evaluation/private.py` companion schemas | manifest hash, extra-field, deployability, and namespace tests | implemented |
| REQ-SFT-006 | The dynamic execution matrix retains every PIDS/variant and executes only an exact admitted frozen configuration | 10 | `sft/builder.py:build_execution_matrix`; `pidsmaker/registry.py` | dynamic matrix smoke; missing admission remains capability-only | implemented; no real executable row admitted |
| REQ-SFT-007 | Offline records contain only past/current public behavior, state, cost, result/failure, and deterministic grouping | 10 | `PublicOfflineRunRecord`; deterministic builders | hash, split, disposition, failure, grouping, and leakage tests | implemented |
| REQ-SFT-008 | Transcripts pair actual tool outcomes and close target claims over evidence visible at that turn | 10 | `DemonstrationExchange`; `CanonicalDemonstrationTrajectory` | ordering, pairing, uncited evidence, supplemental result, and choice tests | implemented |
| REQ-SFT-009 | Private teacher output is strict and cannot force a unique target when public evidence is ambiguous | 10 | `evaluation/teacher.py:StrictTeacherSelectionParser` | rationale, out-of-set target, and ambiguity tests | implemented |
| REQ-SFT-010 | Canonical/OpenAI exports are deterministic, deployable, assistant-loss-only, and report coverage/rejection without success inflation | 10 | demonstration exporter, sanitizer, coverage builder, synthetic script | JSONL round trip, loss mask, no-admission success count, AutoDL smoke | implemented; real coverage pending admitted data |
