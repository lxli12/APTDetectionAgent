# Script layout

Requirement mapping: REQ-REPRO-001..003, REQ-GIT-001..003.

Formal `train_agent.sh`, `test_agent.sh`, and `scripts/remote/` owned-run tools are
delivered in their mapped implementation phases. Phase 0 provides only the
standard-library governance checker; it does not claim that runtime entrypoints are
implemented.
